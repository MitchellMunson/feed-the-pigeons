import { ScreenIdDisplayPipe } from './screen-id-display.pipe';

describe('ScreenIdDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ScreenIdDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
