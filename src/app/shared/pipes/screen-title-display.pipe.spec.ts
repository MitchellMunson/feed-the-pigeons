import { ScreenTitleDisplayPipe } from './screen-title-display.pipe';

describe('ScreenTitleDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ScreenTitleDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
